The app is my simple desktop app with the features of:
-saving fragment of PDF file to other file
-saving fragment of PDF file to separate-page-files

The app is tested on windows system

To build the app (generate jar) use terminal command:
        mvn clean package

To run the app use terminal command:
        java -jar target/my-pdf-utils-1.0-SNAPSHOT-onejar.jar

